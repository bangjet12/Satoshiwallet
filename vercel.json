{
  "buildCommand": "cd frontend && yarn build",
  "outputDirectory": "frontend/build",
  "functions": {
    "backend/api/**/*.py": {
      "runtime": "python3.12"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/backend/api/:path*"
    }
  ]
}
